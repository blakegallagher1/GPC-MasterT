{
  "name": "gpc-mastert",
  "private": true,
  "version": "0.1.0",
  "description": "Agent-first monorepo scaffold",
  "scripts": {
    "lint": "echo 'lint placeholder'",
    "test": "bash scripts/verify-structure.sh",
    "build": "pnpm -r build",
    "test:all": "pnpm -r build && pnpm -r test",
    "smoke": "bash tests/smoke/run.sh"
  }
}
